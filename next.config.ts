/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Disables React Strict Mode
  images: {
    domains: ["i.ibb.co", "i.ibb.co.com"], // Add the hostname here
  },
};

export default nextConfig;